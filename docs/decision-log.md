# 决策日志

## 2026-05-29
- 只优化小程序端，不再把 Web 端逻辑混进来。
- 首页天气链路从 `pages/home/index.js` 下沉到 `services/home_weather.js`，页面只保留生命周期和 `setData`。
- `profile` 页收口为 `utils/profile_view.js`，页面只负责展示与交互。
- 保持现有功能行为不变，优先降低页面层耦合和后续维护成本。
- PB能量舱页面新增 `utils/fueler_state.js`，集中处理仿真结果摘要、补给标签、热力图焦点和参数提示，`pages/fueler/index.js` 只保留事件、缓存和画布调用。
- 重写 `utils/fueler_view.js` 的中文提示文案，清理历史乱码，但不改变仿真公式。
- PB能量舱仿真刷新时合并摘要与热力图状态，减少一次滑块变更后的 `setData` 调用，降低小程序视图同步压力。
- 训练计划服务曾尝试拆分 `utils/plan_schedule.js` 和 `services/plan_progress.js`，但微信开发者工具热重载出现新模块未注册问题；为保证稳定，改为不新增训练计划运行时模块，保留 `services/plan.js` 内部私有函数分区。
- 重写小程序 `README.md`，用当前真实目录和分层约定替换历史乱码内容。

## 2026-06-01
- 首页改为“训练优先”的仪表盘布局：顶部快捷入口、主进度卡、今日训练、PB能量舱入口、赛事轻卡和天气轻卡分层呈现，降低首屏视觉负担。
- 首页绿色使用从“大面积卡片底色”调整为“关键进度和行动强调”，赛事与天气降为深色轻卡，避免首屏过亮和信息抢焦点。
- 首页二次微调：压缩顶部快捷入口高度，在主进度卡加入线性进度条，给赛事横滑区域增加右侧缓冲与渐隐，天气建议框进一步减重。
- 今日训练卡强化为主操作区：增加待打卡/已完成状态胶囊、主任务条、主打卡按钮和“查看计划”轻入口，缩短首页操作路径。
- 自定义 tabBar 图标继续使用纯 CSS 实现，但统一为“圆角发光底座 + 线性符号”，避免引入图片资源，同时提升底部导航的精致度。
- 首页按“高频优先”完成重排：顶部改为天气摘要，中部为今日训练 Hero 卡并内嵌微进度条，目标赛事卡迁出为单行倒计时轻提示，首页移除 PB能量舱入口。
- 计划页新增“智能备赛工具箱 · PB能量舱”入口，承接低频高认知推演操作。
- 首页再次收敛为“真实数据驾驶舱”：保留天气轻条，放大今日训练 Hero 的项目、距离与配速，把计划完成度缩成微进度条；新增“本周训练概览”卡片，但只使用训练计划和打卡状态推导，避免展示没有真实数据支撑的平均配速或路线推荐。
- 首页天气卡升级为“训练环境诊断”：继续使用高德实况温湿度，在本地计算露点；风险标签按低、中、高三档展示，建议框采用暗底与橙色强调线。蓝色只保留给湿度标识，避免破坏首页黑、白、黄、绿主色体系。
- 高德天气 Key 从公开配置中移出：仓库只保留 `weather-provider.private.example.js`，本地真实 Key 写入被忽略的 `weather-provider.private.js`，避免推送 GitHub 时泄露密钥。
- 训练页按“计划驾驶舱 + 课表”重排：赛前减量指南默认折叠，选课安全提示改为弹窗入口；当前周默认展开，已完成训练行降噪，今日未跑训练高亮；总览大绿卡拆分数字与单位，强化仪表盘层级。
